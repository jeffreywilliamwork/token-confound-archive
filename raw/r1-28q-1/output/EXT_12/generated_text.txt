
Okay, so I need to prove that every finite integral domain is a field. Hmm, let's start by recalling the definitions. An integral domain is a commutative ring with unity where there are no zero divisors. A field is a commutative ring with unity where every non-zero element has a multiplicative inverse. So, the key difference is that in a field, every non-zero element is invertible, whereas an integral domain just ensures that there are no zero divisors. 

Since the integral domain is finite, maybe I can use some properties of finite sets. I remember that in finite sets, injective implies surjective. Maybe that can help. Let me think. Suppose R is a finite integral domain. Let's take a non-zero element a in R. I need to show that a has an inverse. Since R is an integral domain, the multiplication by a map, which sends x to ax, should be injective. Because if ax = ay, then a(x - y) = 0, and since a is non-zero and there are no zero divisors, x - y must be 0, so x = y. Therefore, this map is injective. Since R is finite, an injective map from R to R must also be surjective. Therefore, for every element b in R, there exists an x such that ax = b. In particular, there exists an x such that ax = 1. That x would be the inverse of a. Therefore, every non-zero element a has an inverse, so R is a field. That seems right. Let me check if I missed anything. The key steps are using the injectivity of multiplication by a non-zero element, then surjectivity because it's finite, hence 1 is in the image, so a has an inverse. Yeah, that works.

Now, using this result to prove that for any prime p, the integers modulo p form a field. Well, Z_p is the ring of integers modulo p. We know that Z_p is a commutative ring with unity. To show it's a field, we can show it's an integral domain, and since it's finite, by the previous result, it's a field. So, why is Z_p an integral domain? Because if p is prime, then if we have two elements a and b in Z_p such that ab ≡ 0 mod p, then p divides ab. Since p is prime, this implies p divides a or p divides b. Therefore, a ≡ 0 mod p or b ≡ 0 mod p. Hence, there are no zero divisors, so Z_p is an integral domain. Since it's finite, by the first part, it's a field. That makes sense.

Next, construct the field with 4 elements. Hmm, I know that finite fields have order p^n where p is prime. Since 4 is 2^2, the field of order 4 should be GF(4), the Galois field. To construct it explicitly, we can take the polynomial ring GF(2)[x] modulo an irreducible polynomial of degree 2. In GF(2), the polynomials are coefficients 0 or 1. The polynomial x^2 + x + 1 is irreducible over GF(2) because it has no roots in GF(2). Plugging in x = 0: 0 + 0 + 1 = 1 ≠ 0. x = 1: 1 + 1 + 1 = 3 ≡ 1 ≠ 0 mod 2. So, it's irreducible. Therefore, GF(2)[x]/(x^2 + x + 1) is a field with 4 elements. The elements can be represented as 0, 1, α, α + 1, where α is a root of x^2 + x + 1. Let me verify that. So, in this field, addition and multiplication are defined modulo 2 and the polynomial x^2 + x + 1. Let's write out the addition and multiplication tables.

First, the elements are {0, 1, α, α + 1}. Addition is component-wise modulo 2. For example, α + α = (1 + 1)α + (coefficients mod 2), but wait, actually, since in polynomial representation, addition is just coefficient-wise mod 2. So, α + α = 0, since 1 + 1 = 0 in GF(2). Similarly, α + (α + 1) = (α + α) + 1 = 0 + 1 = 1. Let's construct the addition table:

Addition Table:
+ | 0   1     α    α+1
---|----------------------------
0 | 0   1     α    α+1
1 | 1   0    α+1   α
α | α  α+1    0    1
α+1| α+1 α     1    0

Wait, let me check. For example, 1 + α = α + 1, right? Because in polynomial terms, 1 + α is just the polynomial 1 + α. Similarly, 1 + (α + 1) = α + (1 + 1) = α + 0 = α. Hmm, maybe I need to write all combinations.

Let me do it step by step. The elements are 0, 1, α, α+1.

Adding 0 to anything leaves it the same.

1 + 1 = 0 (mod 2).

1 + α = 1 + α (which is α + 1 if we consider ordering, but actually, in GF(4), addition is just polynomial addition, so 1 + α is just the element represented as 1 + α, which is α + 1 since coefficients are mod 2 and we can rearrange terms. Wait, but in GF(4), the elements are 0, 1, α, α + 1. So, 1 + α is indeed α + 1. Similarly, 1 + (α + 1) = (1 + α) + 1 = (α + 1) + 1 = α + (1 + 1) = α + 0 = α.

α + α = 2α, but since coefficients are mod 2, 2α = 0.

α + (α + 1) = (α + α) + 1 = 0 + 1 = 1.

Similarly, α + 1 + α + 1 = (α + α) + (1 + 1) = 0 + 0 = 0.

So the addition table is correct as above.

Now for multiplication. Since we're working modulo the polynomial x^2 + x + 1, which is irreducible over GF(2), we have the relation α^2 + α + 1 = 0, so α^2 = -α -1. But in GF(2), -1 is equivalent to 1, so α^2 = α + 1.

Therefore, we can compute products using this relation. Let's compute the multiplication table.

Multiplication Table:
* | 0   1     α    α+1
---|----------------------------
0 | 0   0     0     0
1 | 0   1     α    α+1
α | 0   α   α+1    1
α+1| 0 α+1    1     α

Let me verify this. 

First, 1 * anything is the element itself.

Now, α * α = α^2 = α + 1 (from the relation above).

α * (α + 1) = α^2 + α = (α + 1) + α = (α + α) + 1 = 0 + 1 = 1.

Similarly, (α + 1) * (α + 1) = α^2 + 2α + 1. But in GF(2), 2α = 0, so this is α^2 + 1 = (α + 1) + 1 = α + (1 + 1) = α + 0 = α.

Wait, let's check that again. (α + 1)^2 = α^2 + 2α + 1. Since we're in GF(2), 2α = 0, so it's α^2 + 1. But α^2 = α + 1, so α^2 + 1 = (α + 1) + 1 = α + (1 + 1) = α + 0 = α. So (α + 1)^2 = α. That seems correct.

Let me check another product. α * (α + 1) = α^2 + α = (α + 1) + α = 1, as before. 

So the multiplication table looks correct. Each non-zero element has an inverse. For example, inverse of α is α + 1, since α * (α + 1) = 1. Similarly, inverse of α + 1 is α. And 1 is its own inverse. So the multiplication table is correct.

Now, to prove that this is the unique field of order 4 up to isomorphism. I know that finite fields of the same order are isomorphic. Since 4 is 2^2, there is exactly one field of order 4 up to isomorphism. But maybe I need to show that any two fields of order 